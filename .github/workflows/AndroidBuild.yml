name: Build Android APK with EAS

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest
    env:
      EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20.x

      - name: Install dependencies
        run: |
          npm install -g expo-cli eas-cli
          npm install

      - name: Configure project files
        run: |
          # Create/update eas.json with Android configuration
          echo '{
            "build": {
              "production": {
                "android": {
                  "buildType": "apk",
                  "gradleCommand": ":app:assembleRelease",
                  "withoutCredentials": false
                }
              }
            },
            "cli": {
              "requireCommit": false,
              "promptToConfigurePushNotifications": false
            }
          }' > eas.json

          # Update app.json with required fields
          if [ -f "app.json" ]; then
            jq '.expo.android.package = "com.HAZdev.dailychecklist" | .expo.android.versionCode = 1' app.json > app.tmp.json && mv app.tmp.json app.json
          else
            echo '{
              "expo": {
                "name": "DailyChecklist",
                "android": {
                  "package": "com.HAZdev.dailychecklist",
                  "versionCode": 1
                }
              }
            }' > app.json
          fi

      - name: Initialize EAS (non-interactive)
        run: |
          # Skip build:configure since we've manually configured everything
          eas init --non-interactive --force --skip-credentials-check
          eas update:configure

      - name: Build Android APK
        run: |
          eas build -p android --profile production --non-interactive --no-wait
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
          EAS_NO_VCS: 1

      - name: Upload APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: android-apk
          path: ./build/android/*.apk
